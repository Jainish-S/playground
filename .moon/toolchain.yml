$schema: 'https://moonrepo.dev/schemas/toolchain.json'

# Python configuration
python:
  version: '3.12.0'

# Node.js for dashboard (future)
node:
  version: '20.10.0'
  packageManager: 'npm'
  npm:
    version: '10.2.0'
